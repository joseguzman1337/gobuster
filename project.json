{
  "name": "repo-repos-gobuster",
  "root": "repos/gobuster",
  "projectType": "library",
  "targets": {
    "status": {
      "executor": "nx:run-commands",
      "options": {
        "command": "git -C repos/gobuster status --short || true"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "fetch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "git -C repos/gobuster fetch --all --prune || true"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "log": {
      "executor": "nx:run-commands",
      "options": {
        "command": "git -C repos/gobuster log --oneline -10 || true"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "manifests": {
      "executor": "nx:run-commands",
      "options": {
        "command": "find repos/gobuster \\( -name package.json -o -name pyproject.toml -o -name Cargo.toml -o -name go.mod -o -name setup.py \\) -not -path '*/node_modules/*' -not -path '*/testdata/*' -not -path '*/fixtures/*' -print | sort"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "go-test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'cd repos/gobuster && go test ./... || true'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "go-build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'cd repos/gobuster && go build ./... || true'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "go-vet": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'cd repos/gobuster && go vet ./... || true'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "go-mod-tidy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'cd repos/gobuster && go mod tidy || true'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "security-go": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'cd repos/gobuster && govulncheck ./... || true'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    },
    "security": {
      "executor": "nx:run-commands",
      "options": {
        "command": "sh -lc 'printf \"security targets: security-go\\n\"'"
      },
      "metadata": {
        "supervisorRequired": true
      }
    }
  },
  "tags": [
    "scope:repos",
    "type:subrepo",
    "lang:go"
  ]
}